---
phase: 10-jira-crud-tests
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified: [tests/e2e/jira.rs]
autonomous: true
---

<objective>
## Goal
Add CRUD tests for Dashboards, Filters, Project Versions, and Project Components. Add read-only tests for Projects, Search, Status, Priorities, Resolutions, Types, and Fields. This brings total Jira entity coverage to ~17 entities.

## Output
- Extended `tests/e2e/jira.rs` with dashboard, filter, version, component CRUD + read-only entity tests
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard, Filter, Version, Component CRUD tests</name>
  <files>tests/e2e/jira.rs</files>
  <action>
    **test_dashboard_crud_lifecycle():**
    - CREATE: `shrug --output json --json '{"name":"E2E dashboard","description":"test"}' jira Dashboards create-dashboard`
    - READ: `shrug --output json jira Dashboards get-all-dashboards`
    - DELETE: `shrug jira Dashboards delete-dashboard --id {id}` (if delete operation exists, otherwise note)

    **test_filter_crud_lifecycle():**
    - CREATE: `shrug --output json --json '{"name":"E2E filter","jql":"project = {project}"}' jira Filters create-filter`
    - READ: `shrug --output json jira Filters get-filter --id {id}`
    - UPDATE: `shrug --json '{"name":"E2E updated filter"}' jira Filters update-filter --id {id}`
    - DELETE: `shrug jira Filters delete-filter --id {id}`

    **test_version_crud_lifecycle():**
    - CREATE: `shrug --output json --json '{"name":"e2e-test-ver","projectId":{id}}' jira "Project versions" create-version`
    - READ: `shrug --output json jira "Project versions" get-version --id {id}`
    - UPDATE: `shrug --json '{"name":"e2e-updated-ver"}' jira "Project versions" update-version --id {id}`
    - DELETE: `shrug jira "Project versions" delete-version --id {id}`

    **test_component_crud_lifecycle():**
    - CREATE: `shrug --output json --json '{"name":"e2e-test-comp","project":"{project}"}' jira "Project components" create-component`
    - READ: `shrug --output json jira "Project components" get-component --id {id}`
    - UPDATE: `shrug --json '{"name":"e2e-updated-comp"}' jira "Project components" update-component --id {id}`
    - DELETE: `shrug jira "Project components" delete-component --id {id}`

    Each test follows the established pattern: setup_profile → create → read → update → delete → teardown.
    Use rate_limit_delay() between API calls.
    If any operation returns an unexpected error (e.g., permission denied), log and skip rather than panic.
  </action>
  <verify>All new CRUD tests pass against live Cloud.</verify>
  <done>Dashboard, Filter, Version, Component CRUD tested</done>
</task>

<task type="auto">
  <name>Task 2: Read-only entity tests</name>
  <files>tests/e2e/jira.rs</files>
  <action>
    Add read-only tests that verify the CLI can list/get common Jira entities:

    **test_list_projects():** `shrug --output json jira Projects search-projects --maxResults 5` — assert exit 0, JSON response
    **test_search_issues():** Already covered by smoke test, but add explicit JQL search here
    **test_list_statuses():** `shrug --output json jira Status search` — assert exit 0
    **test_list_priorities():** `shrug --output json jira "Issue priorities" search-priorities` — assert exit 0
    **test_list_resolutions():** `shrug --output json jira "Issue resolutions" search-resolutions` — assert exit 0
    **test_list_issue_types():** `shrug --output json jira "Issue types" get-issue-all-types` — assert exit 0
    **test_list_fields():** `shrug --output json jira "Issue fields" get-fields` — assert exit 0

    Each test: setup_profile → run read operation → assert exit 0 and valid JSON → teardown.
    These are simple GET operations with no cleanup needed.
  </action>
  <verify>All read-only tests pass against live Cloud.</verify>
  <done>7 read-only entity tests pass</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- `src/**` — no source code changes (executor fix was in 10-01)
- `tests/e2e/harness.rs`, `tests/e2e/smoke.rs`, `tests/e2e/auth.rs` — stable

## SCOPE LIMITS
- Jira entities only (Confluence is Phase 11)
- Skip tests that require admin permissions with a clear message
</boundaries>

<verification>
- [ ] All new CRUD and read-only tests pass
- [ ] All 16 prior tests still pass
- [ ] No orphaned resources after test run
</verification>

<output>
After completion, create `.paul/phases/10-jira-crud-tests/10-02-SUMMARY.md`
</output>
