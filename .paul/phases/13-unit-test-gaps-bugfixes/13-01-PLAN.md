---
phase: 13-unit-test-gaps-bugfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/auth/credentials.rs, src/config.rs, src/helpers.rs, src/main.rs, src/spec/model.rs, src/cli.rs, src/cmd/tree.rs, src/completions.rs, src/logging.rs]
autonomous: true
---

<objective>
## Goal
Fix 6 clippy warnings, fix 2 known bugs (+search deprecated API, +create --project routing), and add unit tests for modules with zero or insufficient coverage.

## Output
- 6 clippy warnings eliminated
- +search uses new search endpoint
- +create receives --project from global flag
- New unit tests for cli.rs, model.rs, tree.rs, completions.rs, logging.rs
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fix clippy warnings and 2 bugs</name>
  <files>src/auth/credentials.rs, src/config.rs, src/helpers.rs, src/main.rs</files>
  <action>
    **src/auth/credentials.rs — Fix 4 clippy warnings:**
    - Line 686: `assert_eq!(store.has_credential(...).unwrap(), false)` → `assert!(!store.has_credential(...).unwrap())`
    - Line 691: `assert_eq!(store.has_credential(...).unwrap(), true)` → `assert!(store.has_credential(...).unwrap())`
    - Lines ~808, ~831: `match orig { Some(v) => env::set_var(..., v), None => {} }` → `if let Some(v) = orig { env::set_var(..., v) }`

    **src/config.rs — Fix 2 clippy warnings:**
    - Lines ~268-269 and ~315-316: field_reassign_with_default — use struct init with `..Default::default()` instead of `let mut config = Default::default(); config.field = value;`

    **src/helpers.rs — Fix +search deprecated API:**
    - Line ~192: Change `find_operation(spec, "searchForIssuesUsingJql", "+search")?` to use `"search-and-reconsile-issues-using-jql"` (the new enhanced search endpoint)
    - Update the search path to use "Issue search" tag if the operation lookup is tag-aware

    **src/main.rs — Fix +create --project flag routing:**
    - In the helper command interception block (around line 536-567):
    - Before dispatching to helpers, inject global shorthand flags into effective_args:
    - If `cli.project.is_some()` and effective_args doesn't already contain "--project", prepend `["--project", &cli.project.unwrap()]` to helper_remaining
    - Same for --assignee, --status etc. so all JQL shorthand flags reach helpers

    Verify: `cargo clippy --tests -- -D warnings` passes with zero warnings.
    Verify: `cargo test --lib` passes (all existing tests).
  </action>
  <verify>
    `cargo clippy --tests -- -D warnings` — zero warnings.
    `cargo test --lib` — all tests pass.
  </verify>
  <done>6 clippy warnings fixed, +search uses new API, +create receives --project</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for zero/low-coverage modules</name>
  <files>src/cli.rs, src/spec/model.rs, src/cmd/tree.rs, src/completions.rs, src/logging.rs</files>
  <action>
    **src/spec/model.rs — Add tests (0→8+):**
    - HttpMethod Display: all 6 variants format correctly (GET, POST, PUT, DELETE, PATCH, OPTIONS)
    - ApiSpec: default/empty spec creation
    - ParameterLocation: all 4 variants

    **src/cli.rs — Add tests (0→6+):**
    - OutputFormat: all 5 variants exist and are parseable
    - ColorChoice: all 3 variants
    - Cli::parse_from() with basic args (--version, --help flags)

    **src/cmd/tree.rs — Strengthen (6→10+):**
    - Empty spec (no tags) produces meaningful output
    - Empty tag (no operations) produces meaningful output
    - Operation with no parameters
    - Operation with no summary/description

    **src/completions.rs — Strengthen (6→8+):**
    - Completions output contains "jira" subcommand
    - Completions output contains global flags

    **src/logging.rs — Strengthen (3→6+):**
    - should_use_color with Auto and NO_COLOR not set (depends on TTY)
    - Verbose level 0 vs 1 vs 2 produces different filter strings

    Verify: `cargo test --lib` passes with new tests.
    Verify: Total unit test count increases by 15+.
  </action>
  <verify>
    `cargo test --lib` — all tests pass.
    `cargo test --lib 2>&1 | grep "test result"` — shows 400+ passed.
  </verify>
  <done>Unit test count increased by 15+, all zero-coverage modules now tested</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- tests/e2e/* — E2E tests stable
- tests/integration.rs — mock tests stable

## SCOPE LIMITS
- Only fix identified clippy warnings and bugs
- Only add tests for identified modules
- Do not refactor working code beyond the specific fixes
</boundaries>

<verification>
- [ ] `cargo clippy --tests -- -D warnings` passes (zero warnings)
- [ ] `cargo test --lib` passes (400+ tests)
- [ ] `cargo test --test integration` passes
- [ ] `cargo test --test e2e` without env vars: all skip
- [ ] +search bug fixed (uses new endpoint)
- [ ] +create bug fixed (--project forwarded to helper)
</verification>

<output>
After completion, create `.paul/phases/13-unit-test-gaps-bugfixes/13-01-SUMMARY.md`
</output>
