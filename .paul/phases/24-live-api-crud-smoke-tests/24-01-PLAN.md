---
phase: 24-live-api-crud-smoke-tests
plan: 01
type: execute
wave: 1
depends_on: ["23-01"]
files_modified:
  - tests/smoke/main.rs
  - tests/smoke/live_api.rs
autonomous: true
---

<objective>
## Goal
Create live API smoke tests that exercise Jira issue and Confluence page CRUD via the installed shrug.exe binary, using E2E credentials.

## Output
- tests/smoke/live_api.rs — Jira issue create/verify/delete + Confluence page create/verify/delete
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Live API CRUD smoke tests</name>
  <files>tests/smoke/live_api.rs, tests/smoke/main.rs</files>
  <action>
    Create tests/smoke/live_api.rs with online tests gated by skip_unless_e2e!().

    Setup helpers:
    - setup_profile(runner, e2e_config) — create temp profile with real credentials, set as default
    - teardown_profile(runner, name) — delete temp profile

    Tests:
    - test_jira_issue_create_and_delete():
      1. skip_unless_e2e!() → (smoke_config, e2e_config)
      2. Create SmokeRunner::with_e2e
      3. Setup profile
      4. Create issue via +create (--project from e2e_config.jira_project, --summary "Smoke test issue")
      5. Verify response contains "key" field
      6. Delete issue via jira issues deleteIssue --issueIdOrKey=KEY
      7. Verify delete succeeds
      8. Teardown profile
      9. Rate limit delay between operations

    - test_confluence_page_create_and_delete():
      1. skip_unless_e2e!() → (smoke_config, e2e_config)
      2. Create SmokeRunner::with_e2e
      3. Setup profile
      4. Look up space ID from e2e_config.confluence_space
      5. Create page via confluence pages createPage with JSON body
      6. Verify response contains "id" field
      7. Delete page via confluence pages deletePage --id=ID
      8. Verify delete succeeds
      9. Teardown profile

    Use ResourceTracker from fixtures.rs for panic-safe cleanup.
    Update tests/smoke/main.rs to add `mod live_api;`
  </action>
  <verify>
    1. cargo test --test smoke live_api -- --test-threads=1 (with E2E env vars set)
    2. cargo test --test smoke -- --test-threads=1 (all pass, live_api skipped if no creds)
    3. cargo clippy --test smoke -- -D warnings (clean)
  </verify>
  <done>Jira + Confluence CRUD validated via installed binary</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- tests/e2e/* — untouched
- src/* — no source code changes
- Existing smoke test modules — read-only (except main.rs for mod declaration)
</boundaries>

<output>
After completion, create .paul/phases/24-live-api-crud-smoke-tests/24-01-SUMMARY.md
</output>
