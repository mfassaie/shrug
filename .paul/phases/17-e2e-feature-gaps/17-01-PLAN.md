---
phase: 17-e2e-feature-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/e2e/features.rs]
autonomous: true
---

<objective>
## Goal
Add E2E tests for 3 CLI features: pagination (--limit), ADF rendering (Markdown input), and verbose logging (--trace, -v).

## Purpose
Closes the last E2E feature gaps in v0.3, testing CLI-level features beyond entity CRUD.

## Output
- 3 new test functions appended to `tests/e2e/features.rs`
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Pagination --limit test</name>
  <files>tests/e2e/features.rs</files>
  <action>
    Append `test_pagination_limit()`:
    1. Run search with `--limit 2` to limit results.
       Use the search_args helper with `--maxResults 2`.
    2. Assert success and JSON response.
    3. Check that the results array has at most 2 entries.
       (If project has fewer than 2 issues, that's fine — just verify the limit is respected.)
    4. Rate limit + teardown.
  </action>
  <verify>Compiles and passes/skips</verify>
  <done>Pagination limit verified</done>
</task>

<task type="auto">
  <name>Task 2: Verbose logging test</name>
  <files>tests/e2e/features.rs</files>
  <action>
    Append `test_verbose_logging()`:
    1. Run a simple command with `-v` flag.
       Use `jira "Issue types" get-issue-all-types` with `-v` prepended.
    2. Assert exit_code 0.
    3. Check stderr contains logging output (e.g. "INFO" or request URL or method name).
       The tracing output goes to stderr.
    4. Rate limit + teardown.

    Append `test_trace_logging()`:
    1. Run same command with `--trace` flag.
    2. Assert exit_code 0.
    3. Check stderr contains request/response details (e.g. HTTP method, URL, or status code).
    4. Rate limit + teardown.
  </action>
  <verify>Compiles and passes/skips</verify>
  <done>Verbose and trace logging verified</done>
</task>

<task type="auto">
  <name>Task 3: ADF Markdown input test</name>
  <files>tests/e2e/features.rs</files>
  <action>
    Append `test_adf_markdown_input()`:
    1. Create an issue with Markdown body via +create helper or direct API with markdown content.
       Use `jira Issues create-issue` with body containing a description field in ADF format
       converted from markdown. Or, simpler: test that creating a comment with plain text body works.
    2. Actually, the simplest test: create an issue, add a comment with ADF body containing
       paragraph text, read it back, verify the text appears.
    3. This tests the ADF round-trip through the CLI.
    4. Clean up: delete issue, teardown profile.

    If ADF input isn't directly testable via CLI flags (markdown → ADF conversion happens
    in +create helper), test the read side: get an issue with description and verify
    the output contains readable text (ADF → terminal rendering).
  </action>
  <verify>Compiles and passes/skips</verify>
  <done>ADF round-trip verified</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Existing functions in features.rs (append only)
- src/*
</boundaries>

<verification>
- [ ] `cargo test --test e2e` compiles without errors
- [ ] `cargo clippy -- -D warnings` produces zero warnings
</verification>

<output>
After completion, create `.paul/phases/17-e2e-feature-gaps/17-01-SUMMARY.md`
</output>
