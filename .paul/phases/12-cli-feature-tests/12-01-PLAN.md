---
phase: 12-cli-feature-tests
plan: 01
type: execute
wave: 1
depends_on: ["10-01"]
files_modified: [tests/e2e/features.rs, tests/e2e/main.rs]
autonomous: true
---

<objective>
## Goal
Test CLI features end-to-end: output formats (table/JSON/YAML/CSV/plain), --dry-run, --fields column selection, JQL shorthand (+search), pagination (--limit), helper commands (+create, +search), and error remediation hints.

## Output
- `tests/e2e/features.rs` with CLI feature tests
</objective>

<tasks>

<task type="auto">
  <name>Task 1: CLI feature E2E tests</name>
  <files>tests/e2e/features.rs, tests/e2e/main.rs</files>
  <action>
    **tests/e2e/main.rs:** Add `mod features;`

    **tests/e2e/features.rs:**

    Reuse setup_profile/teardown_profile pattern.

    **test_output_format_json():**
    - Run search with `--output json`, assert valid JSON response

    **test_output_format_table():**
    - Run search with `--output table`, assert output contains column-like structure

    **test_output_format_yaml():**
    - Run search with `--output yaml`, assert output contains YAML markers

    **test_output_format_csv():**
    - Run search with `--output csv`, assert output contains comma-separated values

    **test_output_format_plain():**
    - Run search with `--output plain`, assert non-empty output

    **test_dry_run_mode():**
    - Run with `--dry-run`, assert output contains "DRY RUN" and no actual API call

    **test_fields_selection():**
    - Run search with `--fields key,summary`, assert output only shows selected fields

    **test_jql_shorthand_search():**
    - Run `shrug jira +search --project {key}`, assert exit 0

    **test_helper_create_and_delete():**
    - Run `shrug jira +create --project {key} --summary "E2E feature test"`, assert issue created
    - Delete the issue for cleanup

    **test_error_remediation_hint():**
    - Run a command that will fail (invalid operation), assert stderr contains "Hint:"

    Each test: setup_profile → run feature → assert → teardown.
  </action>
  <verify>All feature tests pass against live Cloud.</verify>
  <done>Output formats, dry-run, fields, JQL shorthand, helpers, error hints all tested</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- `src/**`, all existing test files
</boundaries>

<verification>
- [ ] All feature tests pass
- [ ] All prior tests still pass
- [ ] Total E2E test count > 35
</verification>

<output>
After completion, create `.paul/phases/12-cli-feature-tests/12-01-SUMMARY.md`
</output>
