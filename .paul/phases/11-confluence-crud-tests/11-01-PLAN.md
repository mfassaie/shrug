---
phase: 11-confluence-crud-tests
plan: 01
type: execute
wave: 1
depends_on: ["09-01", "09-02", "10-01"]
files_modified: [tests/e2e/confluence.rs, tests/e2e/main.rs]
autonomous: true
---

<objective>
## Goal
Create E2E tests for Confluence entities: Space CRUD, Group CRUD, Template CRUD, Search (CQL), and read-only Users. Covers the top entities available in the Confluence Cloud API spec.

## Output
- `tests/e2e/confluence.rs` with CRUD and read-only tests
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Confluence CRUD and read tests</name>
  <files>tests/e2e/confluence.rs, tests/e2e/main.rs</files>
  <action>
    **tests/e2e/main.rs:** Add `mod confluence;`

    **tests/e2e/confluence.rs:**

    Reuse setup_profile/teardown_profile pattern from jira.rs.

    **test_space_crud_lifecycle():**
    - CREATE: `shrug --output json --json '{"key":"E2ETEST","name":"E2E Test Space"}' confluence Space create-space`
    - UPDATE: `shrug --json '{"name":"E2E Updated Space"}' confluence Space update-space --spaceKey E2ETEST`
    - DELETE: `shrug confluence Space delete-space --spaceKey E2ETEST`
    - Note: Space creation may fail if key already exists. Handle gracefully.

    **test_search_content():**
    - Run: `shrug --output json confluence Search search-by-c-q-l --cql "type = page" --limit 5`
    - Assert exit 0 and valid JSON

    **test_get_current_user():**
    - Run: `shrug --output json confluence Users get-current-user`
    - Assert exit 0 and valid JSON

    **test_list_groups():**
    - Run: `shrug --output json confluence Group get-groups`
    - Assert exit 0 and valid JSON

    If any Confluence operation returns unexpected errors (permission, not implemented), log and skip rather than fail.
    Each test uses setup_profile/teardown, skip_unless_e2e!, rate_limit_delay.
  </action>
  <verify>All Confluence tests pass against live Cloud.</verify>
  <done>Confluence entities tested: Space CRUD, Search, Users, Groups</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- `src/**`, `tests/e2e/harness.rs`, `tests/e2e/jira.rs`, `tests/e2e/auth.rs`, `tests/e2e/smoke.rs`
</boundaries>

<verification>
- [ ] All Confluence tests pass
- [ ] All prior tests (26) still pass
</verification>

<output>
After completion, create `.paul/phases/11-confluence-crud-tests/11-01-SUMMARY.md`
</output>
