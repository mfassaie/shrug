# PAUL Handoff

**Date:** 2026-03-23
**Status:** Paused — context limit approaching. v0.3 milestone created, Phase 13 ready to plan.

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** shrug — a dynamic CLI for Atlassian Cloud (Jira, Jira Software, Confluence)
**Core value:** Users and AI agents can interact with Atlassian Cloud products from the command line without context-switching to a browser.

---

## Current State

**Version:** v0.3.0 (Test Coverage & Entity Expansion milestone)
**Phase:** 13 of 17 — Unit Test Gaps + Bug Fixes + Clippy
**Plan:** Not started

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Ready for first PLAN]
```

---

## What Was Done (This Session)

### v0.2 E2E Validation milestone — COMPLETE
- Phase 9: E2E test harness (ShrugRunner, ResourceTracker with Drop, skip macro), network spec fetching (`shrug cache refresh`, 1,227 ops), auth workflow tests (profile CRUD, env vars, first-run)
- Phase 10: Jira CRUD tests (Issues, Comments, Worklogs, Filters, Dashboards, Versions, Components + 7 read-only). Fixed URL resolution bug (credential site over spec placeholder). Added run_json_with_body/run_with_body helpers.
- Phase 11: Confluence tests. Upgraded to v2 API spec (212 ops). Page CRUD + Spaces, Blog Posts, Labels read tests. Fixed URL path prefix preservation.
- Phase 12: CLI feature tests (5 output formats, dry-run, JQL shorthand, helpers, error hints). Documented 2 bugs (+search deprecated API, +create flag routing).
- Tagged v0.2.0, pushed to origin.

### v0.3 milestone created
- 5 phases (13-17): unit test gaps, Jira Platform top 20, Jira Software full, Confluence top 20, E2E feature gaps
- BitBucket and JSM excluded from roadmap
- User approved, ready to plan Phase 13

---

## What's Next

**Immediate:** `/paul:plan` for Phase 13 — Unit Test Gaps + Bug Fixes + Clippy

Phase 13 scope:
- Add unit tests for cli.rs (0 tests) and spec/model.rs (0 tests)
- Strengthen completions.rs (6→15+), cmd/tree.rs (6→15+), logging.rs (3→10+)
- Fix +search helper: update to use search-and-reconsile-issues-using-jql endpoint
- Fix +create helper: --project global flag routing to helper parser
- Fix 6 clippy warnings in src/auth/credentials.rs and src/config.rs

**After that:** Phases 14-17 (entity expansion + feature gaps)

---

## Key Context

- **Live site:** falkonr.atlassian.net (credentials in `.env.e2e`, gitignored)
- **Env vars need export:** `set -a; source .env.e2e; set +a`
- **Products:** Jira (`shrug jira`, 620 ops), Jira Software (`shrug jira-software`, 95 ops), Confluence (`shrug confluence`, 212 ops v2)
- **BitBucket and JSM excluded** from all testing and roadmap
- **Jira search:** `search-and-reconsile-issues-using-jql` under "Issue search" tag
- **Global flags** (--json, --output, --project) must prepend before subcommand (clap trailing_var_arg)
- **Confluence v2 spec:** `openapi-v2.v3.json` (not swagger.v3.json). Server URL has `/wiki/api/v2` path prefix.
- **resolve_base_url()** combines credential site domain + spec URL path prefix
- **40 E2E tests**, 388 unit, 7 integration = 435 total
- **Profile cleanup:** tests create e2e-* profiles with PID suffix, delete on teardown. Some may orphan on panic.

## Known Bugs (Phase 13 scope)

1. `src/helpers.rs` +search: uses deprecated `searchForIssuesUsingJql` endpoint → HTTP 410
2. `src/helpers.rs` or `src/main.rs` +create: `--project` captured by clap as global flag, not forwarded to helper
3. `src/auth/credentials.rs`: 4 clippy warnings (bool_assert_comparison, single_match)
4. `src/config.rs`: 2 clippy warnings (field_reassign_with_default)

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (0 of 5 phases complete) |
| `tests/e2e/harness.rs` | ShrugRunner, E2eConfig, skip_unless_e2e!, RunResult |
| `tests/e2e/jira.rs` | 14 Jira Platform E2E tests |
| `tests/e2e/confluence.rs` | 5 Confluence E2E tests |
| `tests/e2e/features.rs` | 9 CLI feature tests |
| `src/spec/registry.rs` | SpecLoader with network fetch, product URLs |
| `src/executor.rs` | resolve_base_url(), HTTP execution |
| `src/helpers.rs` | +create, +search, +transition (bugs here) |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume`
3. It will suggest `/paul:plan` for Phase 13

---

*Handoff created: 2026-03-23*
