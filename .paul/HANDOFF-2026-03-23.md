# PAUL Handoff

**Date:** 2026-03-23
**Status:** Paused — Phase 9 complete, Phase 10 ready

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** shrug — a dynamic CLI for Atlassian Cloud (Jira, Confluence, BitBucket, JSM, Jira Software)
**Core value:** Users and AI agents can interact with Atlassian Cloud products from the command line without context-switching to a browser.

---

## Current State

**Version:** v0.2.0 (E2E Validation milestone)
**Phase:** 10 of 12 — Jira CRUD Tests
**Plan:** Not started

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Ready for first PLAN]
```

---

## What Was Done (This Session)

- Completed v0.1 MVP milestone (archived, tagged v0.1.0)
- Created v0.2 E2E Validation milestone (4 phases: 9-12)
- **Phase 9 complete** (3 plans, all audited and unified):
  - 09-01: E2E test harness (ShrugRunner, ResourceTracker with Drop, skip_unless_e2e! macro, 4 smoke tests)
  - 09-02: Network spec fetching (cache → network → bundled), `shrug cache refresh` command (1,015 operations across 5 products)
  - 09-03: 9 auth workflow E2E tests (profile CRUD, env var auth, first-run detection, --profile override, auth status)
- 13 E2E tests passing against live Atlassian Cloud (falkonr.atlassian.net)
- Git commit: 734abed

---

## What's In Progress

- Nothing in progress. Clean transition point.

---

## What's Next

**Immediate:** `/paul:plan` for Phase 10 — Jira CRUD Tests
- Top 30 Jira entities with full CRUD (create, read, update, delete) against live Cloud
- Uses the E2E harness, ShrugRunner, and ResourceTracker from Phase 9

**After that:**
- Phase 11: Confluence CRUD Tests (top 30 entities)
- Phase 12: CLI Feature Tests (output formats, pagination, helpers, JQL, ADF)

---

## Key Context

- **Live site:** falkonr.atlassian.net (credentials in `.env.e2e`, gitignored)
- **Env vars need `export`:** Use `set -a; source .env.e2e; set +a` (not just `source`)
- **Jira search endpoint:** `search-and-reconsile-issues-using-jql` under "Issue search" tag. Old endpoint removed (HTTP 410).
- **Profile required for auth:** CLI needs a profile before env var auth works. Tests create temp profiles.
- **run_json() prepends** `--output json` before subcommand (clap trailing_var_arg issue)
- **Bundled specs are placeholders:** Must run `shrug cache refresh` before real operations work
- **6 pre-existing clippy warnings** in src/auth/credentials.rs and src/config.rs (not introduced by this session)
- **.env.e2e has non-variable lines** (lines 9, 13) causing harmless "Client: command not found" during sourcing

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (1 of 4 phases complete) |
| `tests/e2e/harness.rs` | ShrugRunner, E2eConfig, skip_unless_e2e!, RunResult |
| `tests/e2e/fixtures.rs` | ResourceTracker with Drop, create_test_issue helper |
| `tests/e2e/smoke.rs` | 4 smoke tests |
| `tests/e2e/auth.rs` | 9 auth workflow tests |
| `src/spec/registry.rs` | SpecLoader with network fetch |
| `.env.e2e` | Live Atlassian Cloud credentials (gitignored) |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume` or `/paul:progress`
3. It will suggest `/paul:plan` for Phase 10

---

*Handoff created: 2026-03-23*
